package com.codeman.test;

import java.text.SimpleDateFormat;
import java.util.Arrays;
import java.util.Date;

public class InsertSort {

	public static void main(String[] args) {
		//int[] arr = {101, 34, 119, 1, -1, 89}; 
		// ???????80000?????????????
		int[] arr = new int[888888];
		for (int i = 0; i < 888888; i++) {
			arr[i] = (int) (Math.random() * 888888); // ???????[0, 8000000) ??
		}

		System.out.println("?????????");
		Date data1 = new Date();
		SimpleDateFormat simpleDateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
		String date1Str = simpleDateFormat.format(data1);
		System.out.println("????????????=" + date1Str);
		
		insertSort(arr); //???ò?????????
		
		Date data2 = new Date();
		String date2Str = simpleDateFormat.format(data2);
		System.out.println("????????????=" + date2Str);
		
		//System.out.println(Arrays.toString(arr));
		
		
		
		
	}
	
	//????????
	public static void insertSort(int[] arr) {
		int insertVal = 0;
		int insertIndex = 0;
		//???for???????????
		for(int i = 1; i < arr.length; i++) {
			//????????????
			insertVal = arr[i];
			insertIndex = i - 1; // ??arr[1]?????????????±?
	
			// ??insertVal ????????λ??
			// ???
			// 1. insertIndex >= 0 ??????insertVal ?????λ????????
			// 2. insertVal < arr[insertIndex] ???????????????????????λ??
			// 3. ??????? arr[insertIndex] ????
			while (insertIndex >= 0 && insertVal < arr[insertIndex]) {
				arr[insertIndex + 1] = arr[insertIndex];// arr[insertIndex]
				insertIndex--;
			}
			// ?????while??????????????λ?????, insertIndex + 1
			// ??????????????????? debug
			//?????????ж??????????
			if(insertIndex + 1 != i) {
				arr[insertIndex + 1] = insertVal;
			}
	
			//System.out.println("??"+i+"?????");
			//System.out.println(Arrays.toString(arr));
		}
		
		
		/*
		
		
		//?????????????????????????
		//??1?? {101, 34, 119, 1};  => {34, 101, 119, 1}
		
		
		//{101, 34, 119, 1}; => {101,101,119,1}
		//????????????
		int insertVal = arr[1];
		int insertIndex = 1 - 1; //??arr[1]?????????????±?
		
		//??insertVal ????????λ??
		//???
		//1. insertIndex >= 0 ??????insertVal ?????λ????????
		//2. insertVal < arr[insertIndex] ???????????????????????λ??
		//3. ??????? arr[insertIndex] ????
		while(insertIndex >= 0 && insertVal < arr[insertIndex] ) {
			arr[insertIndex + 1] = arr[insertIndex];// arr[insertIndex]
			insertIndex--;
		}
		//?????while??????????????λ?????, insertIndex + 1
		//??????????????????? debug
		arr[insertIndex + 1] = insertVal;
		
		System.out.println("??1?????");
		System.out.println(Arrays.toString(arr));
		
		//??2??
		insertVal = arr[2];
		insertIndex = 2 - 1; 
		
		while(insertIndex >= 0 && insertVal < arr[insertIndex] ) {
			arr[insertIndex + 1] = arr[insertIndex];// arr[insertIndex]
			insertIndex--;
		}
		
		arr[insertIndex + 1] = insertVal;
		System.out.println("??2?????");
		System.out.println(Arrays.toString(arr));
		
		
		//??3??
		insertVal = arr[3];
		insertIndex = 3 - 1;

		while (insertIndex >= 0 && insertVal < arr[insertIndex]) {
			arr[insertIndex + 1] = arr[insertIndex];// arr[insertIndex]
			insertIndex--;
		}

		arr[insertIndex + 1] = insertVal;
		System.out.println("??3?????");
		System.out.println(Arrays.toString(arr)); */
		
	}

}
